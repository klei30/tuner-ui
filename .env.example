# Tuner-UI Docker Environment Configuration
# Copy this file to .env and fill in your values before running docker-compose

# ===== Required: API Keys =====
# Tinker API Key (required for training)
# Get your API key from the Tinker platform
TINKER_API_KEY=your_tinker_api_key_here

# ===== Required: Security =====
# PostgreSQL password (generate a secure random password)
POSTGRES_PASSWORD=change_me_to_secure_password

# Secret key for session encryption (generate with: openssl rand -hex 32)
SECRET_KEY=change_me_generate_with_openssl_rand_hex_32

# Encryption key for HuggingFace token storage
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change_me_generate_with_fernet

# ===== Optional: Frontend Configuration =====
# API URL as seen from the browser (not from within Docker)
# For local Docker: http://localhost:8000
# For production with nginx: https://your-domain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ===== Optional: Backend Configuration =====
# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:80

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Environment (development, staging, production)
ENVIRONMENT=production

# ===== Optional: Monitoring =====
# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ===== Quick Start =====
# 1. Copy this file: cp .env.example .env
# 2. Generate secrets:
#    - POSTGRES_PASSWORD: openssl rand -hex 16
#    - SECRET_KEY: openssl rand -hex 32
#    - ENCRYPTION_KEY: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# 3. Add your TINKER_API_KEY
# 4. Run: docker-compose up -d
# 5. Access: http://localhost:3000
